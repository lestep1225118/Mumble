# Mumble — runtime deps for production (e.g. Vercel).
# For local file analysis with librosa, also install: requirements-optional.txt

flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

beautifulsoup4==4.12.2
lxml==5.1.0

requests==2.31.0

# Optional at runtime (code degrades gracefully if missing)
openai==1.6.1
